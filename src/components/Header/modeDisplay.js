Function modeDisplay() {
    
    return <div className="modeDisplay">when game mode is selected feed the mode key here eg. 'Easy' and use that to call the right constant,
    displaying the core data about the current game. *what I want to happen*
    </div>
}

export default modeDisplay;